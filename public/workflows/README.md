Place ComfyUI API workflow JSON files in this directory.

For example, an image model configured with workflow name `flux-dev-fp8` will load:

`/workflows/flux-dev-fp8.json`

A video model configured with workflow name `wan-i2v` will load:

`/workflows/wan-i2v.json`

Export the workflow from ComfyUI using API Format JSON.
